<%= form_with(model: organ, local: true) do |form| %>
  <% if organ.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organ.errors.count, "error") %> prohibited this organ from being saved:</h2>

      <ul>
      <% organ.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
