<%= form_with(model: tabulator, local: true) do |form| %>
  <% if tabulator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tabulator.errors.count, "error") %> prohibited this tabulator from being saved:</h2>

      <ul>
      <% tabulator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
