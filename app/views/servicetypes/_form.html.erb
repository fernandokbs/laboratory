<%= form_with(model: servicetype, local: true) do |form| %>
  <% if servicetype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(servicetype.errors.count, "error") %> prohibited this servicetype from being saved:</h2>

      <ul>
      <% servicetype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
