<%= form_with(model: pcr_type, local: true) do |form| %>
  <% if pcr_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pcr_type.errors.count, "error") %> prohibited this pcr_type from being saved:</h2>

      <ul>
      <% pcr_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
